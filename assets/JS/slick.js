$(document).ready(function() {
    $('#slick-carousele').slick({
        prevArrow: $('#prev'), /* custom arrow */
        nextArrow: $('#next'), /* custom arrow */
        dots: false,
        infinite: false,
        speed: 300,
        arrows: true,
        autoplay: false,
      });
    });